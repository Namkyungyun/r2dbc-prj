server:
  port: 0

spring:
  profiles:
    active: dev
  application:
    name: info-service
  cloud:
    config:
      uri: http://localhost:8888


management:
  endpoints:
    web:
      exposure:
        include: refresh, health, busrefresh, info


